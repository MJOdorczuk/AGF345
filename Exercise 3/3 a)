# Calculation of Alfvén Velocity

mu_0 = 4 * np.pi * 1e-7  # Permeability of free space (N/A^2)
B = CHOOSE IT  # Magnetic field in the magnetotail (T)
n = CHOOSE IT # Plasma density in the magnetotail (m^3)
m_p = 1.67e-27  # Mass of a proton (kg)

# Calculate the mass density (rho) in the magnetotail
rho = m_p * n  # mass density (kg/m^3)

# Calculate the Alfvén speed (v_A)
v_A = B / np.sqrt(mu_0 * rho)

V_A # in m/s
